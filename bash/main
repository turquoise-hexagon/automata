die() {
    echo "$1" >&2
    exit 1
}

helper() {
    declare -n var=$1

    [[ $2 ]] || die "$usage"

    { printf -v var '%d' "$2" &> /dev/null || ((var < 0)); }
        die "error : '$2' isn't a valid positive integer"
}

# vim: ft=sh
